using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Helloorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //чебупеля
        print("Hello,World");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
